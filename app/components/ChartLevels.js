var React = require('react');
var PropTypes = React.PropTypes;

var ChartLevels = React.createClass({

  render: function() {
    return (
      <div />
    );
  }

});

module.exports = ChartLevels;
