var React = require('react');
var PropTypes = React.PropTypes;

var SectionToggle = React.createClass({

  render: function() {
    return (
      <div />
    );
  }

});

module.exports = SectionToggle;
